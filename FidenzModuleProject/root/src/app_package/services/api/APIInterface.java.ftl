package ${packageName}.services.api;

/**
 * Created by Choota.
 */

public interface APIInterface {
	
	// you may need following imports on build.gradle
	/*
		compile 'com.google.code.gson:gson:2.7'
    	compile 'com.squareup.retrofit2:retrofit:2.1.0'
    	compile 'com.squareup.retrofit2:converter-gson:2.1.0'  

	*/

	// add these imports to your project
	/*
		import retrofit2.Call;
		import retrofit2.http.GET;
		import retrofit2.http.POST;
	*/

	// following is a sample api interface, this will not work, just for demostration
	/*	
	    @GET("/v1/account/contracts")
	    Call<ResponseBody> getContractsList();
	*/
}
